#pragma once

// sol3 lets the embedder override various build knobs via a sol/config.hpp
// header on the include path. We don't need any non-default options for
// now, so this file is intentionally empty — it just satisfies sol.hpp's
// '#include <sol/config.hpp>' line.
